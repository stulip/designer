/**
 *
 * @author tangzehua
 * @sine 2019-09-05 17:37
 */

const config = {
    // 视图大小
    screenSize: {
        width: 375,
        height: 812,
    },
    // 视口大小
    viewportSize: {
        width: 375,
        height: 812,
    },
    // 视口放大率
    viewportScale: 6,
};

export const createConfig = (options)=> {
    return config;
};
