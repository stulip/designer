
.ds-design {
    .item-layout {

        .title {
            color: #415058;
        }

        .bg-content {
            align-items: center;
            flex-direction: column;
            display: flex;
            -webkit-box-pack: justify;
            justify-content: space-between;
            min-height: 22px;
            padding-top: 4px;
        }

    }
}
