
.ds-design {
    .item-layout {

        .title {
            color: #415058;
        }

        .bg-content {
            flex-direction: column;
            display: flex;
            -webkit-box-pack: justify;
            min-height: 22px;
            padding-top: 4px;

        }

    }
}
